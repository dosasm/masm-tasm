{
  "name": "js-dos",
  "description": "resources to use in vscode webview",
  "scripts": {
    "clean": "rm -rf ./out/*.*",
    "build": "npm run clean && gulp && tsc -p ./ --sourceMap false --declaration false",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-replace": "^1.0.0",
    "js-dos": "^6.22.59",
    "typescript": "^3.9.7"
  },
  "dependencies": {}
}

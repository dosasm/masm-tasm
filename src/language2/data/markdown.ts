/**
 * Re-export HoverFromMarkdown data source from the original language module.
 */

import { HoverFromMarkdown } from '../../language/hoverFromMarkdown';
import { keywordType } from '../../language/Hover';

export { HoverFromMarkdown, keywordType };
